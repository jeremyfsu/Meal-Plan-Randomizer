<h1>Listing dishes</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Leftovers</th>
  </tr>

<% @dishes.each do |dish| %>
  <tr>
    <td><%=h dish.name %></td>
    <td><%=h dish.leftovers %></td>
    <td><%= link_to 'Show', dish %></td>
    <td><%= link_to 'Edit', edit_dish_path(dish) %></td>
    <td><%= link_to 'Destroy', dish, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New dish', new_dish_path %>